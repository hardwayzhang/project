# Pulsar容灾容错测试用例文档

## 概述

本文档详细说明了针对Pulsar组件的容灾容错测试用例及其预期结果。这些测试用例从主项目容灾容错的角度设计，确保在主项目与副项目通信过程中，即使遇到各种故障场景，系统仍能保持稳定运行。

## 测试环境要求

- Go 1.21 或更高版本
- Apache Pulsar Go客户端 v0.12.1
- Pulsar服务器（可选，部分测试可以在无服务器环境下验证逻辑）

## 测试用例详细说明

### 测试用例1: 连接失败和自动重连

**测试目的**: 验证客户端在连接失败时能够自动检测并尝试重连。

**测试步骤**:
1. 使用无效的Pulsar服务URL创建客户端
2. 观察客户端的连接状态
3. 等待一段时间，观察重连行为

**预期结果**:
- ✅ 初始连接应该失败（因为URL无效）
- ✅ 如果客户端创建成功，重连机制应该被激活
- ✅ 客户端应该按照配置的`ReconnectInterval`持续尝试重连
- ✅ 重连尝试应该记录在日志中
- ✅ 即使连接失败，客户端不应该panic或崩溃

**验证点**:
- `IsConnected()`方法应该正确反映连接状态
- 重连goroutine应该正常运行
- 重连间隔应该符合配置

---

### 测试用例2: 消息发送失败重试机制

**测试目的**: 验证当消息发送失败时，客户端能够按照配置进行重试。

**测试步骤**:
1. 创建客户端并配置重试参数（MaxRetries=3, RetryBackoff=1s）
2. 尝试发送消息
3. 观察重试行为和耗时

**预期结果**:
- ✅ 如果连接正常，消息应该成功发送
- ✅ 如果发送失败，应该重试最多3次
- ✅ 每次重试之间应该有退避时间（1秒、2秒、3秒...）
- ✅ 重试总耗时应该约为: 初始失败时间 + 1s + 2s + 3s
- ✅ 重试耗尽后应该返回明确的错误信息

**验证点**:
- 重试次数应该不超过`MaxRetries`
- 退避时间应该符合`RetryBackoff * attempt`
- 错误信息应该包含重试次数和最终失败原因

---

### 测试用例3: 网络中断恢复

**测试目的**: 验证客户端在网络中断时能够检测到并自动恢复。

**测试步骤**:
1. 创建客户端并建立连接
2. 模拟网络中断（关闭producer）
3. 触发重连机制
4. 等待重连完成
5. 验证连接状态

**预期结果**:
- ✅ 网络中断后，`IsConnected()`应该返回`false`
- ✅ 自动重连机制应该被触发
- ✅ 重连应该按照配置的间隔进行
- ✅ 如果网络恢复，客户端应该能够重新连接
- ✅ 重连后`IsConnected()`应该返回`true`
- ✅ 重连后应该能够正常发送和接收消息

**验证点**:
- 连接状态检测应该准确
- 重连逻辑应该正确执行
- 重连后功能应该完全恢复

---

### 测试用例4: 服务端故障恢复

**测试目的**: 验证当Pulsar服务端故障时，客户端能够持续尝试重连，并在服务端恢复后正常工作。

**测试步骤**:
1. 创建客户端并建立连接
2. 模拟服务端故障（关闭producer和consumer）
3. 触发重连
4. 等待重连尝试
5. 检查重连状态

**预期结果**:
- ✅ 服务端故障时，客户端应该立即检测到连接断开
- ✅ 客户端应该按照`ReconnectInterval`持续尝试重连
- ✅ 重连尝试应该记录在日志中
- ✅ 如果服务端恢复，客户端应该能够成功重连
- ✅ 重连后所有功能（发送、接收）应该恢复正常
- ✅ 重连过程不应该阻塞主业务逻辑

**验证点**:
- 故障检测应该及时
- 重连尝试应该持续进行
- 服务端恢复后应该能够正常工作

---

### 测试用例5: 消息确认机制

**测试目的**: 验证消息被成功处理后能够正确确认，避免消息重复处理。

**测试步骤**:
1. 发送测试消息
2. 接收消息
3. 确认消息
4. 验证消息不会被重复接收

**预期结果**:
- ✅ 消息应该能够成功发送
- ✅ 消息应该能够成功接收
- ✅ 消息确认应该成功执行
- ✅ 确认后的消息不应该再次被接收（除非是新的消息）
- ✅ 确认操作不应该抛出异常

**验证点**:
- `Acknowledge()`方法应该正常工作
- 消息ID应该正确
- 消息内容应该完整

---

### 测试用例6: 超时处理

**测试目的**: 验证当操作超时时，客户端能够正确返回超时错误，不会无限等待。

**测试步骤**:
1. 配置操作超时时间（OperationTimeout=5s）
2. 使用较短的context超时（2s）发送消息
3. 观察操作是否在超时时间内返回

**预期结果**:
- ✅ 当操作超时时，应该返回`context.DeadlineExceeded`错误
- ✅ 超时时间应该符合配置的`OperationTimeout`或context超时（取较小值）
- ✅ 不应该无限等待
- ✅ 超时后应该立即返回，不继续阻塞

**验证点**:
- 超时错误应该正确返回
- 实际耗时应该小于等于超时时间
- 超时后资源应该正确释放

---

### 测试用例7: 并发操作容错

**测试目的**: 验证多个goroutine同时进行发送和接收操作时，系统能够正确处理，不会出现数据竞争。

**测试步骤**:
1. 创建客户端
2. 启动10个goroutine并发发送消息
3. 启动10个goroutine并发接收消息
4. 统计成功和失败的数量

**预期结果**:
- ✅ 多个goroutine并发操作应该能够正常工作
- ✅ 不应该出现数据竞争（race condition）
- ✅ 不应该出现panic
- ✅ 消息应该能够正确发送和接收
- ✅ 重试机制在并发场景下应该正常工作
- ✅ 所有goroutine应该正常完成

**验证点**:
- 使用`go test -race`应该不报告数据竞争
- 成功发送和接收的消息数量应该合理
- 不应该有goroutine泄漏

---

### 测试用例8: 连接池管理

**测试目的**: 验证多个客户端实例能够正确管理连接，连接断开时能够正确清理和重建。

**测试步骤**:
1. 创建3个客户端实例
2. 验证所有客户端都连接成功
3. 关闭所有客户端
4. 验证资源已正确清理

**预期结果**:
- ✅ 多个客户端实例应该能够独立创建和连接
- ✅ 每个客户端应该有独立的producer和consumer
- ✅ 关闭客户端时应该正确清理资源
- ✅ 连接池应该能够正确管理多个连接
- ✅ 不应该有资源泄漏

**验证点**:
- 每个客户端应该独立工作
- 关闭时不应该有错误
- 资源应该完全释放

---

### 测试用例9: 故障转移机制

**测试目的**: 验证当主Pulsar服务不可用时，客户端能够切换到备用服务（如果配置了多个服务URL）。

**测试步骤**:
1. 创建客户端（注意：实际故障转移需要在Pulsar客户端配置中设置多个URL）
2. 模拟主服务故障
3. 触发重连
4. 观察故障转移行为

**预期结果**:
- ✅ 主服务故障时，客户端应该检测到连接断开
- ✅ 如果配置了多个服务URL，应该尝试连接到备用服务
- ✅ 故障转移应该对应用透明
- ✅ 转移后应该能够正常发送和接收消息
- ✅ 转移过程应该记录在日志中

**验证点**:
- 故障检测应该及时
- 备用服务连接应该尝试
- 转移后功能应该正常

**注意**: 完整的故障转移功能需要在Pulsar客户端配置中设置多个服务URL，这需要在`ClientOptions`中配置。

---

### 测试用例10: 消息丢失检测

**测试目的**: 验证当消息发送失败且重试耗尽时，系统能够检测到消息丢失并记录错误。

**测试步骤**:
1. 配置重试参数（MaxRetries=2）
2. 建立连接
3. 模拟连接中断
4. 尝试发送消息
5. 观察错误信息

**预期结果**:
- ✅ 当连接中断时，消息发送应该失败
- ✅ 应该按照配置进行重试（最多2次）
- ✅ 重试耗尽后应该返回明确的错误信息
- ✅ 错误信息应该包含重试次数和失败原因
- ✅ 错误应该被正确记录，便于后续分析和处理

**验证点**:
- 错误信息应该详细且有用
- 重试次数应该符合配置
- 错误应该能够被上层应用捕获和处理

---

### 测试用例11: 资源清理

**测试目的**: 验证客户端关闭时能够正确清理所有资源，包括producer、consumer和连接。

**测试步骤**:
1. 创建客户端
2. 记录资源状态（producer、consumer、client）
3. 关闭客户端
4. 验证资源已清理

**预期结果**:
- ✅ 关闭客户端时应该正确关闭producer
- ✅ 关闭客户端时应该正确关闭consumer
- ✅ 关闭客户端时应该正确关闭底层连接
- ✅ 所有goroutine应该正确退出
- ✅ 不应该有资源泄漏
- ✅ 关闭操作应该返回成功（无错误）

**验证点**:
- 所有资源应该被正确释放
- 不应该有goroutine泄漏
- 关闭后不应该有残留连接

---

### 测试用例12: 配置验证

**测试目的**: 验证无效的配置能够被拒绝，有效的配置能够被正确应用。

**测试步骤**:
1. 使用无效配置（空URL）创建客户端
2. 验证配置被拒绝
3. 使用有效配置创建客户端
4. 验证配置值被正确应用

**预期结果**:
- ✅ 无效配置应该被拒绝并返回错误
- ✅ 有效配置应该被正确应用
- ✅ 配置值应该能够在运行时正确使用
- ✅ 默认配置应该提供合理的默认值

**验证点**:
- 配置验证应该严格
- 配置值应该正确传递和使用
- 默认配置应该可用

---

## 运行测试

### 运行所有测试

```bash
go test -v ./...
```

### 运行特定测试用例

```bash
go test -v -run TestCase1_ConnectionFailureAndReconnect
```

### 运行并发测试（检测数据竞争）

```bash
go test -race -v ./...
```

### 运行测试并生成覆盖率报告

```bash
go test -cover -coverprofile=coverage.out ./...
go tool cover -html=coverage.out
```

## 测试结果评估标准

### 通过标准

每个测试用例应该满足以下标准才能被认为是通过的：

1. **功能正确性**: 所有预期功能都应该正常工作
2. **错误处理**: 错误应该被正确捕获和处理
3. **资源管理**: 不应该有资源泄漏
4. **并发安全**: 不应该有数据竞争
5. **日志记录**: 重要事件应该被正确记录

### 失败处理

如果测试失败，应该：

1. 检查Pulsar服务器是否运行
2. 检查网络连接是否正常
3. 查看详细日志输出
4. 验证配置是否正确
5. 检查是否有资源限制

## 容灾容错能力总结

通过以上测试用例，Pulsar客户端应该具备以下容灾容错能力：

1. **自动重连**: 连接断开时自动尝试重连
2. **重试机制**: 操作失败时自动重试
3. **超时控制**: 防止操作无限等待
4. **故障检测**: 及时检测连接和操作故障
5. **资源管理**: 正确管理连接和资源
6. **并发安全**: 支持并发操作
7. **错误处理**: 提供详细的错误信息
8. **配置灵活**: 支持灵活的配置选项

## 注意事项

1. **Pulsar服务器**: 部分测试需要Pulsar服务器运行，如果服务器不可用，相关测试会被跳过
2. **网络环境**: 网络中断测试可能需要特殊环境或工具
3. **时间敏感**: 某些测试涉及超时，需要确保测试环境时间准确
4. **资源限制**: 并发测试可能受系统资源限制影响

## 扩展建议

1. **集成测试**: 添加与真实Pulsar服务器的集成测试
2. **性能测试**: 添加性能基准测试
3. **压力测试**: 添加高负载场景测试
4. **监控指标**: 添加监控指标收集和验证
5. **故障注入**: 使用故障注入工具进行更真实的测试
